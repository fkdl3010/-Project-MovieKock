<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >


<mapper namespace="com.koreait.movie.dao.Requestdao">
	
	<select id="request_view" parameterType="int" resultType="com.koreait.movie.dto.CommentDto">
		SELECT faq_no, faq_title, user_no, faq_date
		  FROM FAQ
	</select>
	
</mapper>